class Solution(object):
    def maxProfit(self, prices):
        maxPrice = max(prices)
        minPrice = min(prices)
        
        
        #find min before max and max after min
        minBeforeMaxPrice = maxPrice
        maxAfterMinPrice = minPrice
        for i in prices:
            if

        
        
prices = [7,1,5,3,6,4]
solution = Solution()
print(solution.maxProfit(prices))